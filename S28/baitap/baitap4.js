const rectangle = {
    width: 10,
    height: 5,
    getArea: function () {
        return rectangle.width * rectangle.height;
    }
};

console.log(`Diện tích hình chữ nhật: ${rectangle.getArea()}`);
